# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing,
# software distributed under the License is distributed on an
# "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
# KIND, either express or implied.  See the License for the
# specific language governing permissions and limitations
# under the License.
#
---
name: airflow-teradata-tests
on:  # yamllint disable-line rule:truthy
  pull_request:
    branches: ['develop']
  schedule:
    - cron: '28 0 * * *'
  push:
    branches: ['main']
jobs:
  teradata-tests:
    name: "Run Teradata Provider Unit and System tests"
    runs-on: "ubuntu-22.04"
    strategy:
      max-parallel: 1
      matrix:
        python-version: ["3.8", "3.9", "3.10", "3.11"]
    steps:
      - name: Cleanup repo
        run: docker run -v "${GITHUB_WORKSPACE}:/workspace" -u 0:0 bash -c "rm -rf /workspace/*"
      - name: "Checkout ${{ github.ref }} ( ${{ github.sha }} )"
        uses: actions/checkout@v4
        with:
          ref: develop
          persist-credentials: false
      - name: "Install Python"
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
          cache-dependency-path: ./dev/breeze/setup*
      - name: Display Python version
        run: python -c "import sys; print(sys.version)"
      - name: "Install required packages"
        run: pip install --user "pipx>=1.2.1";pip install -U pytest;python -m pip install time-machine;pip install pytest-asyncio;
             pip install pytest-md-report;pip install cryptography;pip install pytest-html;
      - name: "Install airflow and teradata provider from sources"
        run: INSTALL_PROVIDERS_FROM_SOURCES="true" pip install ".[teradata]" --constraint https://raw.githubusercontent.com/apache/airflow/constraints-main/constraints-source-providers-${{ matrix.python-version }}.txt      
      - name: "Verifying Airflow version"
        run: airflow version;      
      - name: "Initialize airflow database"
        run: airflow db migrate;
      - name: "Run Unit tests"
        run:   pytest --html=report.html --self-contained-html  tests/providers/teradata/operators/test_teradata.py;      
      - name: Init new repo in folder where report generated and commit generated files
        run: |
          cd /home/runner/work/airflow/airflow/
          git init
          git add -A
          git config --local user.email "satishchinthanippu@gmail.com"
          git config --local user.name "Satish Ch"
          git commit -m 'deploy'
      - name: Force push to destination branch
        uses: ad-m/github-push-action@v0.5.0
        with:
          # Token for the repo
          # Can be passed in using $\{{ secrets.GITHUB_TOKEN }}
          github_token: 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
          # Destination branch to push changes
          branch: gh-pages
          # Use force push to fully overwrite the destination branch
          force: true
          # We have to push from the folder where files were generated.
          # Same were the new repo was initialized in the previous step
          directory: /home/runner/work/airflow/airflow/
